/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "www.google.com",
      "www.duckduckgo.com",
      "www.wikimedia.org",
      "upload.wikimedia.org",
    ],
  },
};

module.exports = nextConfig;
